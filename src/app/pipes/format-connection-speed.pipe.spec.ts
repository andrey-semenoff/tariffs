import { FormatConnectionSpeedPipe } from './format-connection-speed.pipe';

describe('FormatConnectionSpeedPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatConnectionSpeedPipe();
    expect(pipe).toBeTruthy();
  });
});
